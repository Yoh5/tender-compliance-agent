# One line ending everywhere. Mixed endings make a whitespace-only diff look
# like a rewrite, which is how a real change gets waved through unread.
* text=auto eol=lf

# Real documents, byte-exact.
*.pdf binary
